#
# ~/.bashrc
#

export SPOTIFY_ID="bf4d2741162d4ed0b0513a724c9b0071"
export SPOTIFY_SECRET="b320f4c8b50943a5944118fea05b4e2a"

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '





export PATH=$PATH:/home/khai/.spicetify
